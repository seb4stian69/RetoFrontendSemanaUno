import { ReloadcomponentDirective } from './reloadcomponent.directive';

describe('ReloadcomponentDirective', () => {
  it('should create an instance', () => {
    const directive = new ReloadcomponentDirective();
    expect(directive).toBeTruthy();
  });
});
